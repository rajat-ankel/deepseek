{
  "version": 2,
  "builds": [
    {
      "src": "index.js", // Entry point for the API
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)", // Catch-all route for API requests
      "dest": "/"
    }
  ]
}
